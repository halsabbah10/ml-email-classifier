# Backend Environment Variables
# Copy this file to backend/.env and update with your values

# Database Configuration
# For localhost development, use 127.0.0.1 instead of localhost to avoid MySQL auth issues
# For Docker, use the service name 'mysql' as hostname
DATABASE_URL=mysql+pymysql://root:your_password@127.0.0.1:3306/email_classifier

# API Configuration
API_PORT=8002

# Frontend Environment Variables (optional)
# Set in frontend/.env if you need to override defaults
# REACT_APP_API_URL=http://localhost:8002

# Docker Notes:
# - Windows users: Ensure Docker Desktop is running with WSL2 backend
# - If PyMySQL version issues occur, the requirements.txt uses stable versions